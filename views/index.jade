extends layout

block content
    div(class="container")
        div(id="editor")
        div(class="messages")
            ol Let's make these tests pass!!!
                li you should be using:
                    ul(class="whiteList")
                li you should not be using:
                    ul(class="blackList")
                li you should have
                    ul(class="structure")
                        li the correct structure